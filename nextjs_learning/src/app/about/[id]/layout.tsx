const AboutPageLayout = ({ children }: { children: React.ReactNode }) => {
  return (
    <div>
      <h1> {children}</h1>
      <span>about feature is these</span>
    </div>
  );
};

export default AboutPageLayout;
